운영체제 
- 커널(운영체제의 핵심영역)
  -> 장치제어(디스크, 입출력장치, CPU), 메모리관리, 프로세스관리
     : 프로세스(프로그램이 실행되서 메모리에 올라가면)
- 리눅스 -> 커널을 말함
- 우분투 리눅스 -> 리눅스 커널 기반의 배포판 운영체제
- 배포판 리눅스 -> 커널에 사용자가 필요한 몇가지 프로그램을 추가한 것
  -> 레드햇(redhat) 리눅스: RHEL(레드햇 Enterprise Linux, 유료), CentOS(무료)
  -> 데비안(debian) 리눅스: 데비안 리눅스, 우분투 리눅스